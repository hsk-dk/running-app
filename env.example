APP_BASE_URL=https://xxx.yyy.zz
DATABASE_PATH=/data/running.db
CORS_ORIGINS=https://xxx.yyy.zz

STRAVA_CLIENT_ID=xxxxxx
STRAVA_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STRAVA_REFRESH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STRAVA_VERIFY_TOKEN=very-random-secret
STRAVA_ATHLETE_ID=xxxxxxxxxxxx
STRAVA_PUSH_SUBSCRIPTION_CALLBACK_URL=https://xxx.yyy.zz/api/webhooks/strava

# Ollama local LLM settings (for AI training suggestions)
# When using docker compose, Ollama runs as a sidecar service and OLLAMA_URL
# defaults to http://ollama:11434 automatically — no change needed here.
# Override only if you want to point to a different Ollama instance.
# OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=llama3

# LLM provider selection:
# - ollama: use local Ollama instance (uses OLLAMA_URL + OLLAMA_MODEL)
# - openai_compatible: use a cloud provider via OpenAI-compatible API
LLM_PROVIDER=ollama

# API key for openai_compatible providers (Google AI Studio, Groq, OpenRouter, etc.)
LLM_API_KEY=

# Base URL for openai_compatible providers
# Google AI Studio default:
LLM_API_URL=https://generativelanguage.googleapis.com/v1beta/openai

# Model name for openai_compatible providers
LLM_MODEL=gemini-2.5-flash
